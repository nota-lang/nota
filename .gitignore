node_modules
dist
docs

# Separate git repo, committed independently:
references/

# Integration harness scratch
integration/.note.emitted.mjs
# Vendored at pack time by scripts/pack-release.ts (the node wasm reader)
packages/compiler/wasm/
