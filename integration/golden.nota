%let Colorized = (props: { children?: unknown }) => {
  let [color, setColor] = createSignal("red");
  return @span[onClick: () => setColor("green")][style: {color: color()}]{@(props.children)};
}

@for (x of ["a", "b"]) {
  - @Colorized{@x}
}
