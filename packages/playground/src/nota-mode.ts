/**
 * Phase T (placeholder until filled below): a lightweight CM6 `StreamLanguage` for Nota's sigils.
 * Replaced with the real tokenizer in phase T; kept as an export so the editor wires it from phase R.
 */
import type { Extension } from "@codemirror/state";

export function notaLanguage(): Extension {
  return [];
}
